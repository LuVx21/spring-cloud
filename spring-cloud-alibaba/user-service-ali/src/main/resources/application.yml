#server:
#  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: "*"

spring:
  application:
    name: user-service-ali
  cloud:
    nacos:
      discovery:
        server-addr: localhost:58848